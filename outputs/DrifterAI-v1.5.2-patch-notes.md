# DrifterAI v1.5.2 Patch Notes

## Changed
- Game status posts now make platform state obvious in the title and description.
- Status messages use clear labels like `OPERATIONAL`, `MAINTENANCE`, `DEGRADED`, or `OUTAGE`.
- Bot version is now `v1.5.2`.

# DrifterAI v1.3.1 Patch Notes

## Changed
- Gemini chat answers now require grounded web sources.
- DrifterAI no longer silently falls back to text-only Groq for web questions when Gemini is configured.
- Railway logs now show which AI provider is active.
- Bot version is now `v1.3.1`.
